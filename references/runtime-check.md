# Runtime Check

## 用途

检查本次运行生成的文件产物是否真的可用。

## 负责

至少检查：文件是否写出、路径是否正确、格式是否合法、内容是否乱码或空白。问题页重点看 `30_Questions/*.md`；对比图重点看 `.canvas` 是否是合法 JSON。

## 不负责

不判断回答是否答题，不判断结构是否过载，也不决定是否补新内容。

## 下一步

产物有效就继续读 `references/quality-check.md`；产物无效则回到当前链路修正，不直接结束。
