# Text Output Reference

## 目的

统一组织最终显示给用户的文字。本文档不生成文件、不判断 route、不修改任务内容，只把已经执行并验证的结果整理成简短、可追踪的回答。

## 何时读取

文件产物必须先通过 `references/runtime-check.md`。全部 task 执行结束后读取本文件，组织最终回答，再交给 `references/quality-check.md` 检查。

## 基本顺序

1. 先给出当前请求的核心结果或结论。
2. 文件 task 给出能力、动作和绝对路径。
3. 仅在确有必要时追加内容长度或阅读密度提醒。
4. 多任务按用户原顺序合并，不重复解释流程。

## 文件回执

| action 与结果 | 输出格式 |
|---|---|
| `create` 成功 | `已使用 <能力> 生成：<绝对路径>` |
| `update` 成功 | `已使用 <能力> 更新：<绝对路径>` |
| `keep` | `已保留原文件，不进行修改：<绝对路径>` |
| 执行失败 | `未生成或更新文件：<明确原因>` |

只有脚本 result 和 Runtime Check 都成功时，才能使用“已生成”或“已更新”。路径必须来自本次实际结果或已经确认的 keep 目标。

## 语言回答

`answer` task 直接输出对题回答，不强制添加机械化前缀。若语言回答与文件 task 同时存在，先回答对应问题，再列文件回执。

## 内容提醒

字数或布局偏重不阻断生成，不自动压缩，不要求用户在生成前选择。任务成功后才追加一条短提醒。

中英文分别估算：中文按汉字及全角字符计数，英文按单词计数；混合文本分别统计，任一项达到对应条件即可提醒。以下都是软判断，不是生成上限：

| 产物 | 偏重参考 |
|---|---|
| Canvas 单个核心节点 | 中文超过约 250 字，或英文超过约 130 词 |
| 整张 Canvas | 中文超过约 1000 字，或英文超过约 500 词 |
| Canvas 布局 | group 宽高比低于约 `0.9`，或核心节点高度超过约 `520px` |
| Canvas 接近文档 | group 宽高比低于约 `0.6` |
| Markdown 或语言回答 | 中文超过约 1000 字，或英文超过约 500 词时，可提示正文较长 |

数字只辅助判断。内容本身适合当前媒介时可以不提醒；明显难以浏览时即使未达到数字也可以提醒。

| 情况 | 提醒 |
|---|---|
| Canvas 内容密集、明显偏纵向 | `提示：这张 Canvas 内容较密，已经接近纵向文档；可用“压缩当前 Canvas”覆盖原文件。` |
| Markdown 正文明显过长 | `提示：这份文档内容较长；可要求压缩当前文档或拆分主题。` |
| 语言回答明显过长 | 用一句话说明可压缩重点，不重复正文 |

提醒只描述已经观察到的问题，不主动扩展学习主题，不增加新的相关概念或任务清单。

## 多任务示例

```text
已完成：
1. 已使用 compare_canvas 更新：<绝对路径>
2. 已使用 question_note 生成：<绝对路径>

提示：第一张 Canvas 内容较密，已经接近纵向文档。
```

## 下一步

固定进入 `references/quality-check.md`。质量检查通过后再发送最终回答。
