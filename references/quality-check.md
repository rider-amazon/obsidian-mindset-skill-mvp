# Quality Check

## 用途

检查这次输出是否真正对题，并且没有越界。

## 负责

至少检查：有没有直接回答用户问题、有没有空洞到只剩概括、有没有堆太多无关信息、有没有乱引未实现能力。若是开放模式，还要检查是否写得散乱发散。

## 不负责

不检查 JSON、Markdown、Canvas 是否损坏，也不检查目标目录是否正确；这些都属于 `runtime-check.md`。

## 下一步

质量通过则结束运行；不通过则在当前模式内收缩或重写回答后再结束。
