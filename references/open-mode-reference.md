# Open Mode Reference

## 用途

定义 open mode 的最小约束，供复杂请求或未命中标准能力的请求使用。

## 负责

默认中文、结构优先、只围绕当前问题展开；必要时可以落盘，但不为了显得完整而扩写无关概念，不默认生成多余图。

## 不负责

不负责标准命令分流，不展开 py 细节，不讨论未来功能规划，也不把未实现能力伪装成标准流程。

## 下一步

默认直接组织回答。若后续真的生成了文件产物，再读 `references/runtime-check.md`；回答结束前一律读 `references/quality-check.md`。
